# --- VISUAL (Última versión) ---
# Usamos la más nueva. Si sale un warning, arreglamos el CÓDIGO, no la librería.
streamlit==1.41.1

# --- MOTOR DE IA (Estándar Industrial) ---
# La 2.3.1 es la versión más robusta para audio hoy en día.
# Las versiones 2.4 y 2.5 rompieron compatibilidad (eso es lo que te daba el error TorchCodec).
torch==2.3.1
torchaudio==2.3.1
torchvision==0.18.1

# --- DEPENDENCIAS ---
numpy<2.0.0
librosa==0.10.2
soundfile
panns-inference==0.1.1
hear21passt==0.0.26
pandas
matplotlib
huggingface-hub
graphviz